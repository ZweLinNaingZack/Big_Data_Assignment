services:
  - type: web
    name: medical-insurance-dashboard
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn --bind 0.0.0.0:$PORT heroku_ready_dashboard:server
    envVars:
      - key: PYTHON_VERSION
        value: 3.9.0